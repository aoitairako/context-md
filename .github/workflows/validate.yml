name: Validate CONTEXT.md
on:
  push:
    branches: [main]
    paths: ['CONTEXT.md', 'tools/validate.py', 'action.yml']
  pull_request:
    paths: ['CONTEXT.md', 'tools/validate.py', 'action.yml']
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./
